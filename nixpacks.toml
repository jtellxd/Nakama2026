[phases.build]
cmds = [
    "pip install -r requirements.txt",
    "python manage.py collectstatic --noinput"
]

[phases.setup]
nixPkgs = ["python311"]

[start]
cmd = "python manage.py migrate --noinput && gunicorn control_asistencia.wsgi --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120"
